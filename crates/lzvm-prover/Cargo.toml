[package]
name = "lzvm-prover"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "lzvm_prover"
path = "src/lib.rs"

[dependencies]
libloading = "0.8"
lzvm-artifacts = { path = "../lzvm-artifacts" }
lzvm-field = { path = "../lzvm-field" }
